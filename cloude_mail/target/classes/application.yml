server:
  # 端口
  port: 8083
spring:
  # 邮件配置
  mail:
    # 邮件服务器地址
    host: smtp.qq.com
    # 协议
    protocol: smtp
    # 编码格式
    default-encoding: utf-8
    # 授权码（在邮箱开通服务时获取）
    password: bxvnagtqxhnabfac
    # 发送者邮箱地址
    username: 670901080@qq.com
    # 端口（不同邮箱端口号不同）
    port: 465
    # 设置ssl
    properties.mail.smtp.ssl.enable: true

  # rabbitmq配置
  rabbitmq:
    # 用户名
    username: guest
    # 密码
    password: guest
    # 服务器地址
    host: 192.168.44.3
    # 端口
    port: 5672
    # 开启手动确认
    listener:
      simple:
        acknowledge-mode: manual